name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Swift format lint
        run: swift format lint --recursive Sources Tests
      - name: SwiftLint
        run: swiftlint
      - name: Swift test
        run: swift test
      - name: Swift build
        run: swift build -c release --product reminders
